{{- if .Values.smoke.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "rbt-data-generator.fullname" . }}-smoke
  labels:
    {{- include "rbt-data-generator.labels" . | nindent 4 }}
    app.kubernetes.io/component: smoke
spec:
  backoffLimit: {{ .Values.smoke.backoffLimit }}
  template:
    metadata:
      labels:
        {{- include "rbt-data-generator.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: smoke
    spec:
      restartPolicy: Never
      {{- include "rbt-data-generator.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "rbt-data-generator.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "rbt-data-generator.waitForPostgres" . | nindent 8 }}
      containers:
        - name: rbt-smoke
          image: {{ include "rbt-data-generator.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["rbt", "smoke"]
          envFrom:
            {{- include "rbt-data-generator.rbtEnvFrom" . | nindent 12 }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: output
              mountPath: /app/output
          {{- with .Values.smoke.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: output
          persistentVolumeClaim:
            claimName: {{ include "rbt-data-generator.outputClaimName" . }}
      {{- with .Values.smoke.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.smoke.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.smoke.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
