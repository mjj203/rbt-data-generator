# MkDocs and theme
mkdocs>=1.5.0
mkdocs-material>=9.0.0

# MkDocs plugins (existing)
mkdocs-mermaid2-plugin>=0.6.0
mkdocs-git-revision-date-localized-plugin>=1.2.0

# API documentation plugin
mkdocs-swagger-ui-tag>=0.6.0

# Export and print plugins
mkdocs-pdf-export-plugin>=0.5.10
mkdocs-print-site-plugin>=2.3.0

# Additional dependencies for markdown extensions
pymdown-extensions>=10.0

# CLI reference generation: docs/_hooks/gen_cli_reference.py shells out to
# `python -m typer` at build time, which needs the rbt package installed
# (pip install -e .) — typer itself comes in as its dependency.
